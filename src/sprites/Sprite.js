export default class Sprite {
    constructor(ctx,x,y) {
        this.x = x;
        this.y = y;
        this.ctx = ctx;
    }

    // this.ctx.fillStyle = "#FF0000";
    // ctx.fillRect(0,height-20,20,20);
}